# Generated with https://github.com/charleskorn/dependabot-config-generator

version: 1
update_configs:
  - package_manager: docker
    update_schedule: daily
    directory: .batect/database
    automerged_updates:
      - match:
          dependency_type: all
          update_type: all
  - package_manager: docker
    update_schedule: daily
    directory: .batect/exchange-rate-service-fake
    automerged_updates:
      - match:
          dependency_type: all
          update_type: all
  - package_manager: docker
    update_schedule: daily
    directory: .batect/international-transfers-service
    automerged_updates:
      - match:
          dependency_type: all
          update_type: all
  - package_manager: ruby:bundler
    update_schedule: live
    allowed_updates:
      - match:
          update_type: all
    directory: /
    automerged_updates:
      - match:
          dependency_type: all
          update_type: all
